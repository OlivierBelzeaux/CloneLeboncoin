<div class="album py-5 bg-light">
  <div class="container">

    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
      <% @announcements.find_each do |announcement| %>
        <div class="col">
          <%= render "shared/card", announcement: announcement %>
        </div>
      <% end %>
    </div>
  </div>
</div>
